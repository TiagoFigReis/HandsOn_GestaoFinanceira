import { Route } from '@angular/router';
import { RelatoriosComponent } from './relatorios/relatorios.component';

export const RelatoriosRoutes: Route[] = [
  {
    path: '', component: RelatoriosComponent
  },
];